import { create } from '@storybook/theming'

import logo from './LOGO.png'

export default create({
  base: 'light',
  brandTitle: '쓱!블로그 가이드',
  brandUrl: 'http://localhost:3000/',
  brandImage: logo
})
